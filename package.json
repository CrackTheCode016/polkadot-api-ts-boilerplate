{
  "name": "polkadot-api-ts-boilerplate",
  "version": "1.0.0",
  "description": "A place to quickly get started with Polkadot API",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc-watch --noClear -p ./tsconfig.json --onSuccess \"node ./dist/index.js\"",
    "postinstall": "npx papi add assethub -n polkadot_asset_hub && npx papi",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "tsc-watch": "^6.2.1",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@polkadot-api/descriptors": "file:.papi/descriptors",
    "polkadot-api": "^1.8.1"
  }
}
